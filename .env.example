# === Twilio (WhatsApp) ===
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=  # tu número o sandbox aprobado
# PUBLIC_BASE_URL=https://your-render-service.onrender.com  # para validación opcional de firma

# === OpenAI ===
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# === Firebase / Firestore ===
GOOGLE_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=  # ruta en el contenedor
FIRESTORE_COLLECTION_PREFIX=milobots

# === Vector store ===
VECTORSTORE_PATH=./vectorstore/index.faiss
DOCS_PATH=./data  # carpeta con fuentes .txt/.md/.html

# === Business logic ===
ALERTS_WHATSAPP_NUMBER=
LEADS_WHATSAPP_NUMBER=
SESSION_IDLE_MINUTES=30
RETENTION_MONTHS=24

# === Security ===
CRON_SECRET=
VALIDATE_TWILIO_SIGNATURE=false